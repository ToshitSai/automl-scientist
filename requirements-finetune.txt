# Optional extras for the fine-tuning module (NOT installed by default).
#
# These are deliberately kept OUT of requirements.txt (Vercel serverless bundle)
# and requirements-local.txt (classical-ML research stack). The finetuning/
# scaffold imports none of them at module load time, so the project runs without
# them. Install only on a GPU host when implementing a real training backend:
#
#   pip install -r requirements-finetune.txt
#
torch>=2.3.0
transformers>=4.44.0
datasets>=2.20.0
trl>=0.11.0
peft>=0.12.0
accelerate>=0.33.0
